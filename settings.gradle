rootProject.name = "MvcApp"
include ':apis:service'
include ':features:app'

dependencyResolutionManagement {
    versionCatalogs {
        testLibs {
            from(files("/gradle/testlibs.versions.toml"))
        }
    }
}